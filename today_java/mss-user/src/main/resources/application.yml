server:
  port: 9072

spring:
  application:
    name: users

  # 默认激活 dev 环境 (Windows)。
  # 部署到宝塔时，在启动参数中添加 --spring.profiles.active=prod 即可切换
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://43.139.169.190:3306/today?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: today
    password: whl13616963610
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 15
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

  data:
    redis:
      host: 43.139.169.190
      port: 6379
      password: redis
      timeout: 5000ms
      jedis:
        pool:
          max-idle: 10
          min-idle: 2
          max-active: 20
          max-wait: -1ms

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    locale: zh_CN
    time-zone: GMT+8

  ai:
    openai:
      api-key: ${dashscope.api-key:sk-5f83eb74be6b4fe49c0a62b03294db1e}
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: ${dashscope.model-name:qwen-max}
          temperature: 0.7

    mcp:
      client:
        enabled: true
        name: my-mcp-client
        version: 1.0.0
        type: SYNC

swagger:
  enable: true

logging:
  level:
    org.apereo.cas.client: DEBUG
    org.springframework.security.cas: DEBUG
    com.zaxxer.hikari: INFO
    org.springframework.jdbc: INFO
    com.microservice.skeleton.user.mapper: DEBUG
    org.springframework.ai.mcp: DEBUG

jwt:
  secret: "mySuperSecretKeyForJWTTokenGeneration2024WithAtLeast32Characters"
  expiration: 86400000

wechat:
  appId: wxc202b4632d79f73b
  appSecret: 90c9703c42a6b1bb62ea023998f956cd

amap:
  api-key: ${AMAP_API_KEY:your-amap-api-key}
